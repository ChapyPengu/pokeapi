apiVersion: v1
kind: Pod
metadata:
  name: multiplayer
  labels:
    name: multiplayer
spec:
  containers:
  - name: multiplayer
    image: oneartec/javipam_chapam_sebapam_2024_psr:latest
    ports:
      - containerPort: 8000
    command: ["bun" , "run" , "./server/index.js"]
